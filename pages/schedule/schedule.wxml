<view class="page">
  <view class="title">
    <text>分享顺序</text>
  </view>
  <view class="sharingListContainer">
    <view class='sharingList' wx:for="{{list}}" wx:key="name" wx:for-item="item">
      <view class="timeLineIconWrap" >
        <image class="timeNodeIcon" src="../../images/timeNode.png"></image>
      </view>
      <view class="photo">
        <image src="{{item.avtarUrl}}"></image>
      </view>
      <view class='personalInfo'>
        <view>{{item.name}}</view>
        <view class="department">{{item.department}}</view>
      </view>
    </view>
  </view>
</view>